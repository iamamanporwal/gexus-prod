{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//": [
    "Deliberately minimal. Nitro's vercel preset emits Build Output API v3 to",
    ".vercel/output, and its generated config.json is authoritative for routing —",
    "redirects/headers/rewrites declared here do not take effect. Asset cache",
    "headers live in vite.config.ts as nitro routeRules instead, which DO end up",
    "in config.json.",
    "",
    "Vercel project settings that must match (they are not expressible here):",
    "  Framework Preset : Other        <- NOT Vite; Vite makes Vercel look for",
    "                                     dist/ and ignore .vercel/output",
    "  Build Command    : npm run build",
    "  Output Directory : (empty)      <- setting this also bypasses .vercel/output",
    "  Install Command  : npm ci",
    "  Node.js Version  : 22.x         (or driven by .node-version)"
  ]
}
